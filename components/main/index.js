import styled from "styled-components";

const Main = styled.div`
  margin: auto;
  width: 100%;
  background: var(--c-bg);
  padding: var(--size-m) 0;
  flex: 1 1 auto;
  overflow-y: auto;
`;

export default Main;
