import styled from "styled-components";

const Form = styled.form`
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: var(--size-m);
  padding: var(--size-s);
  background-color: #fff;
`;

export default Form;
